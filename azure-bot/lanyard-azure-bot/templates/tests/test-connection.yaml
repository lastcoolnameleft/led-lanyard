apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lanyard-azure-bot.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "lanyard-azure-bot.name" . }}
    helm.sh/chart: {{ include "lanyard-azure-bot.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "lanyard-azure-bot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
