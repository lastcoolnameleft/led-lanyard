apiVersion: v1
kind: Service
metadata:
  name: {{ include "lanyard-azure-bot.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "lanyard-azure-bot.name" . }}
    helm.sh/chart: {{ include "lanyard-azure-bot.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "lanyard-azure-bot.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
